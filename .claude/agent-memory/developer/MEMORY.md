# Developer Agent Memory

## Project Environment
- .NET SDK 10.0.103 installed (as of 2026-03-05), runtime 10.0.3
- All projects updated to `net10.0` TFM (were originally net9.0)
- No global.json in repo

## Known Issues & Fixes
- **SQLite file lock on Windows**: Microsoft.Data.Sqlite uses connection pooling. In test Dispose(), must call `SqliteConnection.ClearAllPools()` before `File.Delete()` or the file remains locked and xUnit marks the test as FAILED.
- For integration tests that use ServiceProvider, dispose the SP first, then clear pools, then delete file.

## Project Structure
- Solution: `NexusAgent.slnx`
- 5 src projects: Nexus.Memory, Nexus.Core, Nexus.Connectors, Nexus.CLI, Nexus.Desktop
- 3 test projects: Nexus.Memory.Tests (103 tests), Nexus.Core.Tests (422 tests, 1 pre-existing env-var failure, 1 skip), Nexus.Integration.Tests (190 tests, 2 pre-existing failures)
- Total: ~1062 tests across all test projects; 3 pre-existing failures (GetApiKey_ReturnsNull_WhenNoKeyAvailable — GEMINI_API_KEY env var is set; DI_OllamaProvider — Ollama not running; PlanStep_NoToolMatched_GroundingInjected_NoHallucination — FakeLlmProvider returns last user message, not grounding)

## Key Files
- DI registration: `src/Nexus.Core/ServiceCollectionExtensions.cs`
- MCP DI registration: `src/Nexus.Connectors/McpServiceCollectionExtensions.cs`
- DB schema: `src/Nexus.Memory/DatabaseInitializer.cs`
- Config: `src/Nexus.Core/Config/NexusConfig.cs`
- LLM client interface: `src/Nexus.Memory/ILlmClient.cs`
- LLM client impl: `src/Nexus.Core/OllamaLlmClient.cs`
- Entity extraction: `src/Nexus.Memory/EntityExtractor.cs`
- Tool executor interface: `src/Nexus.Core/IToolExecutor.cs`
- MCP client manager: `src/Nexus.Connectors/McpClientManager.cs`
- Tool registry: `src/Nexus.Connectors/ToolRegistry.cs`
- MCP tool executor: `src/Nexus.Connectors/McpToolExecutor.cs`
- Schema validator interface: `src/Nexus.Core/Abstractions/ISchemaValidator.cs`
- Schema validator impl: `src/Nexus.Connectors/SchemaValidator.cs`

## Patterns Learned
- Raw string literals with `$"""` cannot have `{{` literal braces when the content contains BOTH `{{literal}}` and `{interpolation}`. Use `$"..."` with `\"` escaping, or `$$"""..."""` with `{{interpolation}}` for clean multiline. Avoid `$"""` when JSON braces are present alongside interpolation.
- Layer hygiene: When AgentService (Core) needs a server name from ToolDefinition (Connectors), add a `GetToolServerName(string) => string.Empty` default-method on IToolExecutor rather than returning `object?` and downcasting in Core (which would break the layer boundary).
- Verification retry logic: In the bounded while-loop, `attempt < maxAttempts` controls retry. When `attempt == maxAttempts` and verification fails, fall through to normal tool-result recording (stepCompleted = true). The [VerificationWarning] text is preserved in the tool result history entry. Test assertions should NOT expect the exhaustion sentinel in this path.
- Nexus.Memory has `InternalsVisibleTo("Nexus.Memory.Tests")` for testing internal static methods.
- DI registration order matters: ModelRouter -> ILlmClient -> EntityExtractor (dependency chain).
- `ILlmClient` uses `sp.GetService<>()` (nullable) not `sp.GetRequiredService<>()` to match nullable constructor.
- MCP SDK v1.1.0: Uses `McpClient.CreateAsync()` (not McpClientFactory). `HttpClientTransport` with `HttpTransportMode.AutoDetect` for SSE/StreamableHttp. `StdioClientTransport` for stdio. `CallToolResult.IsError` is `bool?`. `EnvironmentVariables` is `IDictionary<string, string?>`.
- Connectors -> Core dependency (one-way). McpServerEntry lives in Core Config. IToolExecutor lives in Core.
- In C# switch expressions, `Array` is unreachable after `IList` because `Array` implements `IList`. Use only `IList` to match both arrays and lists.
- Spectre.Console SelectionPrompt choices: strings like "[a]", "[s]" are parsed as markup tags and throw `InvalidOperationException`. Use double-escaped `[[a]]`, `[[s]]` etc. The returned value from `console.Prompt()` will also be `[[a]] Allow once` (not `[a]`), so switch/StartsWith must match `[[a]]`. Always use `TestConsole().Interactive()` for SelectionPrompt tests; keys are pushed with `console.Input.PushKey(ConsoleKey.DownArrow)` and `PushKey(ConsoleKey.Enter)`.
- PathValidator strict-distance guard (AC-7): guard ONLY fires when `IsWithinAllowedDirectory(normalized)` is true. Paths outside allowed dirs (e.g., Z:\ vs C:\) are legitimate "wrong root" corrections; the strict guard must not block them. Gate condition: `originalWithinAllowed && distance < _strictDistance`. Test isolation: use deeply nested fake dirs (4+ segments) to push full-path Fuzz.Ratio score reliably below 90, since temp dir prefix is very long and dilutes the score delta.

## Sprint Progress
- Sprint 1 Day 1: COMPLETE (embedding service)
- Sprint 1 Day 2 (US-1.2): COMPLETE (LLM entity extraction with 3-level fallback)
- Sprint 1 Day 3: COMPLETE (memory context builder + semantic search + decay)
- Sprint 1 Day 4: E2E tests + DI factory tests + hotfix validation (AC-1,2,3,8,9,11,12)
- Sprint 2 Day 3 (US-2.4 AC-1-4): COMPLETE (MCP SDK integration, IToolExecutor, McpClientManager rewrite, ToolRegistry update)
- Sprint 3 Day 5 (US-3.2 AC-4,5,6,7,9): COMPLETE (CompressSummariesAsync, RelevanceDecay archival hook, AgentService background archival, CLI archive/compress commands, 7 new tests). 170 tests total.
- Sprint 3 Day 6 (US-3.3 AC-1-7): COMPLETE (MCP persistence + CLI disconnect/servers commands, 5 new tests). 175 tests total.
- Schema Validation (AC-1-6): COMPLETE (ISchemaValidator + SchemaValidator + AgentService integration + DI + config flags, 11 new tests). 186 tests total (excluding pre-existing env-var failure).
- Phase 8.3 (Bounded Plan-Step Retry Semantics, AC-1-8): COMPLETE (StepExecutionMaxAttempts config field, ConfigValidator, GetToolSchema DIM on IToolExecutor, McpToolExecutor override, BuildStepPrompt+DescribeRequired+BuildArgsTemplate helpers in AgentService, ExecutePlanAsync/ExecutePlanStreamAsync bounded while-loop rewrite, 4 new ConfigValidatorTests + 4 new AgentServicePlanExecutionTests). AgentServicePlanExecutionTests: 13 total. ConfigValidatorTests: 63 total. Tool result format changed: "[Tool result for step N]\n{result}" (was "[PlanStep N Result]: {result}").
- Phase 9 Wave D (AC-8, AC-9, AC-10 finish): COMPLETE. GetToolDefinition + GetToolServerName default-methods on IToolExecutor, McpToolExecutor overrides, IVerificationCatalog + IToolVerifier DI in McpServiceCollectionExtensions, IToolVerifier wired into ExecuteToolWithTimeoutAsync (pre+post snapshot), AC-9 verification retry in ExecutePlanAsync + ExecutePlanStreamAsync, toolVerifier ctor param in AgentService, ServiceCollectionExtensions updated. 7 new tests: AgentServiceVerificationTests (5) + AgentServicePlanVerificationRetryTests (2). Total: 170 integration tests (1 pre-existing DI failure).
- Phase 9 Wave E (AC-11 finish): COMPLETE. nexus.yaml.example — uncommented 4 planner_context_* keys + added tool_verification_enabled + verification_snapshot_timeout_seconds (with section headers). SKILL.md — updated IToolExecutor/IToolPlanner/AgentService/McpToolExecutor/McpConfig entries; added Catalog/ directory block; added IPlannerContextBuilder/IToolVerifier/IVerificationCatalog Abstractions entries; added PlannerContext.cs Models entry; added PlannerContextBuilder + SyntheticMarkers Services entries; updated test counts. AgentService.cs — updated ExtractVerificationReason XML doc to match spec + replaced AC-8 comment with detailed 4-line timeout/OCE explanation. McpServiceCollectionExtensions.cs — added DI order comment before IVerificationCatalog registration. Build: clean (6 pre-existing NU1903 only). Tests: 2 pre-existing failures only (env-var + Ollama DI).
- Sprint 10 AC-7 (PathValidator stale-state guard): COMPLETE. Added PathValidatorStrictDistance (default 90, range 50-100) to McpConfig. Added ValidatePathValidatorStrictDistance to ConfigValidator. PathValidator: added _strictDistance field, FindBestMatchWithScore overload returning full-path score via out param, existence-wins guard (STEP A: if match found but original exists → return original), strict-distance guard (STEP B: only fires when path is within allowed dirs but missing AND full-path score < _strictDistance → reject fuzzy correction). Key insight: strict guard must be gated on IsWithinAllowedDirectory(normalized) to preserve existing "wrong-root" catalog correction behavior. Tests: 3 new PathValidatorTests + 1 theory + 1 default-value in ConfigValidatorTests. Nexus.Core.Tests: 94 total. All pre-existing failures unchanged.
- Sprint 10 AC-1 (Planner heuristic gate): COMPLETE. PlannerInvocationHeuristic static class (Services/): de-accent+length check, greeting HashSet, imperative-verb regex (bilingual), path-pattern regex, file-extension regex. Returns (ShouldPlan, Reason) — never throws (try/catch → fallback_default_allow). McpConfig: PlannerHeuristicEnabled (default true) + PlannerHeuristicMinLength (default 16). ConfigValidator: ValidatePlannerHeuristicMinLength (range 1-200). AgentService: heuristic wired into ChatAsync + ChatStreamAsync plan-gate — heuristicAllow flag gates the planner block. nexus.yaml.example: 2 new keys. Tests: 16 new PlannerInvocationHeuristicTests + 5 new ConfigValidatorTests theory cases. Existing plan-execution tests updated to set PlannerHeuristicEnabled=false (CreateAgent helper + 2 other test files). Nexus.Core.Tests: 339 total (1 pre-existing env-var failure). Integration: 3 pre-existing failures unchanged.
- Sprint 10 AC-6 (Summary prompt hardening): COMPLETE. SummaryFailureAnalyzer internal static class in Nexus.Core/Services/: single-pass Analyze() over IReadOnlyList<ConversationMessage>, counts 5 sentinel types (VerificationWarning, PlanStep+Exceeded+attempts, [Tool *failed:, [PermissionDenied], [DoomLoop]), caps ExcerptedReasons at last 3; BuildGroundingMessage() returns "" when no failures, otherwise injects [PlanResult] with non-zero count lines only. AgentService: wired in BOTH ExecutePlanAsync and ExecutePlanStreamAsync just before the "All steps complete. Summarize..." message. Key fix: StepExecution_ExceedsMaxAttempts_LogsErrorAndSkips test updated to use StartsWith("[PlanStep ") to count sentinels rather than Contains("Exceeded"), since grounding message echoes the reason text. Tests: 5 new SummaryFailureAnalyzerTests (unit) + 2 new AgentServiceSummaryHardeningTests (integration).
- Sprint 10 AC-3 (IPermissionGate contract + PermissionConfig): COMPLETE. IPermissionGate.cs (Abstractions/): PermissionRequest record, PermissionDecision enum (5 values), PermissionGateResponse record, PermissionFeedback record. NexusConfig: Permission property + PermissionConfig class + PermissionToolRule class. ConfigValidator: ValidatePermissionAction helper + Permission.Tools dict walk in Validate(). PermissionPatternExtractor (Services/): internal static, walks SnapshotSpec.Args JSONPath values or falls back to common key names (path/source/destination/file_path/filename), returns ["*"] on any failure. Tests: 5 PermissionConfigTests + 9 PermissionPatternExtractorTests + 6 ConfigValidatorTests additions. Nexus.Core.Tests: 372 total (1 pre-existing env-var failure, 1 pre-existing skip).
- Sprint 10 AC-4 (CliPermissionGate + PersistentPermissionStore + AutoApprovePermissionGate): COMPLETE. PersistentPermissionStore (Core/Services/): file-backed JSON store at ~/.nexus/permissions.json, atomic rename writes, SemaphoreSlim in-process lock, Base64(cwd) directory keys, LookupAsync/AllowAsync/UpsertAsync/ListAsync. AutoApprovePermissionGate (Core/Services/): inlined tier detection (mirrors ToolCapabilityResolver regex), Full-tier→Allow, small-tier→Deny("non-interactive"). CliPermissionGate (CLI/): 5-choice prompt for Full tier, 3-choice for small, session cache HashSet, persistent store writes, glob matching via FileSystemName.MatchesSimpleExpression. CRITICAL: Spectre.Console markup bracket fix — choice labels must use [[a]] not [a] (escaped brackets) or SelectionPrompt throws InvalidOperationException. switch pattern must use StartsWith("[[a]]"). Desktop: AutoApprovePermissionGate registered in App.axaml.cs. Spectre.Console.Testing 0.49.1 added to Integration.Tests. Tests: 2 AutoApprovePermissionGateTests (Core.Tests) + 5 CliPermissionGateTests (Integration.Tests). Total: 374 Core.Tests (1 pre-existing env-var failure), 177 Integration.Tests (1 pre-existing DI failure).
- Sprint 10 AC-5 (AgentService permission gate wire-in): COMPLETE. SyntheticMarkers: added PermissionDeniedMarker + PermissionDeniedPrefix + extended Prefixes array 8→9. SummaryFailureAnalyzer: replaced PermissionDeniedLiteral private const with SyntheticMarkers.PermissionDeniedMarker. AgentService ctor: added 11th (IPermissionGate?) + 12th (IVerificationCatalog?) optional params after IToolVerifier?; added _permissionGate + _verificationCatalog readonly fields; gate wire-in in ExecuteToolWithTimeoutAsync AFTER _argumentValidator block BEFORE _toolVerifier pre-snapshot; serverName hoisted earlier (shared by gate + verifier); ResolveConfigAction private helper added. ServiceCollectionExtensions: added permissionGate: sp.GetService<IPermissionGate>() + verificationCatalog: sp.GetService<IVerificationCatalog>() to AgentService factory. Safety invariant: gate throws → log Warning + fall through to Allow (never deny on gate bug). Tests: 9 new AgentServicePermissionGateTests. Total: 374 Core.Tests (1 pre-existing), 187 Integration.Tests (1 pre-existing).
- Sprint 10 AC-9 (SKILL.md update): COMPLETE. Updated IVerificationCatalog.cs entry (Destructive field), AgentService.cs entry (11th+12th ctor params, AC-1/AC-5/AC-6 wire-ins), SyntheticMarkers.cs entry (Prefixes 8→9, PermissionDeniedMarker/Prefix), NexusConfig McpConfig (3 new fields: PlannerHeuristicEnabled, PlannerHeuristicMinLength, PathValidatorStrictDistance), NexusConfig top-level (Permission property + PermissionConfig class). New entries: IPermissionGate.cs (Abstractions/), PlannerInvocationHeuristic.cs, SummaryFailureAnalyzer.cs, PermissionPatternExtractor.cs, PersistentPermissionStore.cs, AutoApprovePermissionGate.cs (all Services/), CliPermissionGate.cs (CLI/), PathValidator.cs (Connectors/). Updated: VerificationCatalogLoader.cs (Destructive DTO field), filesystem.yaml (13 rules, 4 new destructive), ConfigValidator.cs (3 new validations), ServiceCollectionExtensions.cs (permissionGate+verificationCatalog), Program.cs (DI), App.axaml.cs (DI placeholder), DI conventions block, NexusConfig csharp class block. Test counts bumped: Core.Tests ~374 total, Integration.Tests ~187 total.
- Sprint 10 Layer 4 (Output Fidelity Verifier, AC-L4-1 through AC-L4-8): COMPLETE. New: OutputFidelityVerifier.cs (Services/) + FidelityResult sealed record — hybrid 3-gram + embedding cosine scoring with benefit-of-doubt (ngrams<3→1.0), null-embedding→1.0 fallback, OCE propagation, outer exception→null. SyntheticMarkers: Prefixes 9→10, FidelityWarningMarker + FidelityWarningPrefix consts. SummaryFailureAnalyzer: Findings extended with 7th positional int FidelityWarnings, detection branch after StepsSkippedNoToolMatch, BuildGroundingMessage "Fidelity failures: N" line. NexusConfig McpConfig: 5 new fields (OutputFidelityVerificationEnabled/MinScore/SubstringWeight/EmbeddingWeight/MaxRetries). ConfigValidator: 4 new validators. ServiceCollectionExtensions: OutputFidelityVerifier singleton + 13th arg in AgentService factory. AgentService: 13th optional ctor param OutputFidelityVerifier?, ExtractReadToolResults(ToolPlan), BuildFidelityGroundingMessage private static helper, Layer 4 wire-in in both ExecutePlanAsync and ExecutePlanStreamAsync. Test fixes: OutputFidelityVerifierTests — ASCII-only test content for n-gram tests; LongToolResult redesigned; SubstringOnly_LlmHallucinates_Fails assertion changed to check SubstringScore < 0.30 (not EmbeddingScore == 0); NgramTooShort uses stopword-only input (1 content token); Normalization test uses ASCII markdown. Tests: OutputFidelityVerifierTests 12 tests (Core.Tests), SummaryFailureAnalyzerTests +2, ConfigValidatorTests +5, AgentServicePlanExecutionTests +2 (Integration.Tests). Key lesson: test data for n-gram tests MUST use ASCII-only content; Normalize() does NOT strip accented chars (only removes [#*`~]+). Integration test internal-class access constraint: SummaryFailureAnalyzer is internal to Nexus.Core — Integration.Tests cannot call it; assert on [FidelityWarning] sentinel text in ConversationHistory instead.
